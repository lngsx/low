Super minimal dotfiles. They're my things to make remote editing and navigation less painful.

## Install

On a fresh VM, one command:

```sh
curl -fsSL https://raw.githubusercontent.com/lngsx/low/main/install.sh | bash
```

Re-running the same command updates the dotfiles to the latest from this repo.
